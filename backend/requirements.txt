# Backend Python dependencies for Singularity
# Pin minimum compatible versions where reasonable; adjust in CI as needed.

fastapi>=0.95.0
uvicorn[standard]>=0.22.0
python-multipart>=0.0.6
pillow>=9.0.0
numpy>=1.22.0
opencv-python-headless>=4.7.0
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.3.1+cpu; platform_system == "Linux"
torchvision==0.18.1+cpu; platform_system == "Linux"
torch>=1.12.0; platform_system != "Linux"
torchvision>=0.13.0; platform_system != "Linux"
grad-cam>=1.4.6
python-dotenv>=1.0.0
requests>=2.28.0

# Optional / dev
# pytest for tests, black for formatting
pytest>=7.0.0
black>=23.0.0
slowapi>=0.1.5
limits>=2.6.0